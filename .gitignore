# Build artifacts
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test

# Coverage output
*.out
coverage.txt

# Go workspace files (local-only, per-developer)
go.work
go.work.sum

# Editor / tool-specific local config
.claude/
.vscode/
.idea/
.DS_Store

# Local, ad-hoc test apps (manual verification scripts, not part of the
# library or its test suite) — kept on disk permanently but never committed.
/local/
